# 设备管理小程序开发与运营手册

这份手册面向不熟悉代码的产品、运营和实施人员。日常工作主要通过微信开发者工具、微信公众平台和云开发控制台完成。

## 一、项目组成

- `pages/index`：设备列表、搜索、筛选、统计
- `pages/add`：新增和编辑设备
- `pages/detail`：设备详情、删除
- `pages/reminder`：到期提醒、订阅授权、测试发送
- `utils/device.js`：设备状态、日期计算、本地缓存和同步
- `utils/cloud.js`：云开发调用
- `utils/config.js`：云环境 ID、订阅模板 ID
- `cloudfunctions/login`：获取微信用户身份
- `cloudfunctions/device`：设备数据和订阅额度
- `cloudfunctions/notify`：定时扫描并发送订阅消息

## 二、首次配置

### 1. 准备账号

需要一个已认证的小程序账号，并确认 AppID 是：

```text
wxec8d958752903b00
```

### 2. 创建云环境

在微信开发者工具中打开项目，点击“云开发”，选择环境：

```text
cloud1-d0gp4f27p361054e5
```

项目已经在 `utils/config.js` 配置该环境 ID；如果更换环境，只修改这个文件。

### 3. 部署云函数

在“云开发 → 云函数”中分别右键部署：

1. `login`
2. `device`
3. `notify`

选择“云端安装依赖”。每次修改云函数代码后，都必须重新部署对应函数。

`device` 云函数首次运行会自动创建 `devices` 和 `device_push` 集合。

### 4. 申请订阅消息模板

在微信公众平台进入“功能 → 订阅消息”，申请设备到期提醒模板。当前模板 ID：

```text
p0XVlOZARY0_Z58SBVQ1VUXVEPmyAW_cm1q86-011qU
```

项目当前按以下字段发送，模板字段必须一致：

| 字段 | 含义 |
|---|---|
| `thing1` | 设备名称 |
| `time3` | 到期日期和时间 |
| `number2` | 剩余或超期天数 |

如果重新申请了模板，需要同时修改 `utils/config.js` 和 `cloudfunctions/notify/index.js`，然后重新部署 `notify`。

### 5. 配置定时触发器

`cloudfunctions/notify/config.json` 当前配置为每 5 分钟执行一次：

```json
"config": "0 */5 * * * * *"
```

修改该文件后重新部署 `notify`。定时器只负责启动云函数，提醒判断和消息发送都在 `notify` 内完成。

## 三、用户使用流程

1. 用户打开小程序，系统静默获取微信身份并同步该用户的数据。
2. 用户添加设备，填写名称、类型、安装日期、使用期限、到期日期和到期时间。
3. 用户进入“更换提醒”，选择提前 7、15、30、60 或 90 天提醒。
4. 用户点击“微信订阅到期提醒”，在微信弹窗中点击允许。
5. 云端每 5 分钟检查一次，符合条件时给设备所属用户发送消息。

设备按微信用户身份隔离：谁录入的设备，就提醒谁。

## 四、提醒规则

- 到期时间精确到分钟，例如 `2026-10-01 14:35`。
- 旧数据没有到期时间时按 `23:59` 处理。
- 默认在到期前 30 天进入提醒范围。
- 已过期设备也符合提醒条件。
- 每个用户每次云函数执行最多发送一条消息。
- 发送成功会消耗一次订阅额度；用户重新授权后会增加额度。

## 五、如何测试

1. 使用真机打开小程序，不要只依赖模拟器。
2. 添加一台 5 分钟内到期的测试设备。
3. 在提醒页点击“微信订阅到期提醒”并允许。
4. 点击“立即测试提醒”。
5. 查看页面显示的“已发送、跳过、失败”数量。
6. 如需测试定时器，等待下一次 5 分钟触发，或在云开发控制台查看 `notify` 日志。

测试阶段 `notify` 使用 `developer` 版本环境；正式发布前把 `MINIPROGRAM_STATE` 改为 `formal` 并重新部署。

## 六、常见问题

### 显示 `sent: 0, skipped: 1`

通常表示没有订阅额度，或设备尚未进入提醒时间范围。重新授权订阅，并把测试设备设置为近期到期。

### `INVALID_WX_ACCESS_TOKEN`

不要在云开发控制台直接运行 `notify` 作为发送测试。使用小程序提醒页的“立即测试提醒”按钮；同时确认云环境已绑定当前小程序。

### `data.xxx.value is empty`

订阅模板字段和 `FIELD_MAP` 不一致。查看错误中提示的字段名，在 `cloudfunctions/notify/index.js` 修改映射和对应值，然后重新部署。

### 定时器没有执行

确认 `notify` 已重新部署、`config.json` 已上传，并在云开发控制台的云函数详情中看到定时触发器。定时器执行后可在日志中搜索 `START RequestId`。

### 用户收不到消息

确认用户在真机上点击过订阅并允许，且订阅额度大于 0。开发版测试时 `MINIPROGRAM_STATE` 必须是 `developer`；正式版使用 `formal`。

## 七、日常改动流程

### 只改页面文字或样式

修改对应 `wxml`、`wxss` 或页面 `js`，在微信开发者工具点击“编译”预览，确认无误后上传代码。

### 改设备字段或提醒规则

同时检查 `pages/add`、`utils/device.js` 和 `cloudfunctions/notify/index.js`，完成真机测试后再上传。

### 改云函数

修改函数目录中的 `index.js` 或 `config.json` 后，必须右键对应云函数并重新部署。云函数日志是排查线上问题的第一入口。

### 更换云环境或模板

先修改 `utils/config.js`，再同步修改 `notify` 中的模板 ID 和字段映射，最后重新部署全部相关云函数并重新真机授权。

## 八、发布前检查

- 云环境 ID 正确
- 三个云函数均已部署最新版本
- `notify` 定时触发器存在且频率正确
- 订阅模板 ID 和字段映射一致
- `MINIPROGRAM_STATE` 已改为 `formal`
- 真机完成登录、设备同步、订阅和消息发送测试
- 微信公众平台版本已提交审核并发布
